# Xyzen backend `.env` example
#
# Notes:
# - This file is an example template. In local dev, copy it to the backend's `.env` and uncomment what you want to override.
# - Prefix: `XYZEN_`
# - Nesting: `_` splits levels; do NOT use `_` inside a single segment.
# - Naming: camelCase segments that match config fields; parsing is case-insensitive.
#
# By default, the backend config has sensible defaults, so everything below is commented out.

# =========================================================================
# Service Core Settings & Security
# =========================================================================
# XYZEN_Title="Xyzen Service"
# XYZEN_Description="FastAPI + MCP integrated service"
# XYZEN_Version="0.1.0"
# XYZEN_Secret="please$#@change&%me*in!production#2024@xyzen%secret^key"
# XYZEN_Env=dev
# XYZEN_Debug=True
# XYZEN_Host=0.0.0.0
# XYZEN_Port=48196
# XYZEN_Workers=1

# =========================================================================
# Logger
# =========================================================================
# XYZEN_Logger_Level=info
# XYZEN_Logger_File_Enabled=False
# XYZEN_Logger_File_Path=/var/log/xyzen.log

# =========================================================================
# Authentication
# =========================================================================
# Provider: casdoor | bohrium | bohr_app
# XYZEN_Auth_Provider=casdoor

# --- Casdoor ---
# XYZEN_Auth_Casdoor_PublicKey="""-----BEGIN CERTIFICATE-----
# ...
# -----END CERTIFICATE-----"""
# XYZEN_Auth_Casdoor_Issuer=http://host.docker.internal:8000
# XYZEN_Auth_Casdoor_JwksUri=http://host.docker.internal:8000/.well-known/jwks
# XYZEN_Auth_Casdoor_Algorithm=RS256
# XYZEN_Auth_Casdoor_Audience=a387a4892ee19b1a2249
# XYZEN_Auth_Casdoor_ClientSecret=f3167664b2c58bca53b04c61807a97db

# --- Bohrium ---
# XYZEN_Auth_Bohrium_PublicKey="""-----BEGIN PUBLIC KEY-----
# ...
# -----END PUBLIC KEY-----"""
# XYZEN_Auth_Bohrium_Issuer=https://host.docker.internal:8000
# XYZEN_Auth_Bohrium_JwksUri=http://localhost:7000/.well-known/jwks
# XYZEN_Auth_Bohrium_Algorithm=RS256
# XYZEN_Auth_Bohrium_Audience=your-bohrium-client-id

# --- BohrApp ---
# XYZEN_Auth_BohrApp_PublicKey=
# XYZEN_Auth_BohrApp_Issuer=https://openapi.dp.tech/openapi/v1/ak/user
# XYZEN_Auth_BohrApp_JwksUri=
# XYZEN_Auth_BohrApp_Algorithm=RS256
# XYZEN_Auth_BohrApp_Audience=your-bohrapp-client-id

# =========================================================================
# Database
# =========================================================================
# Engine: postgres | sqlite
# XYZEN_Database_Engine=postgres

# --- Postgres ---
# XYZEN_Database_Postgres_Host=localhost
# XYZEN_Database_Postgres_Port=5432
# XYZEN_Database_Postgres_User=postgres
# XYZEN_Database_Postgres_Password=postgres
# XYZEN_Database_Postgres_DBName=postgres
# XYZEN_Database_Postgres_MinConnections=1
# XYZEN_Database_Postgres_MaxConnections=10

# --- SQLite ---
# XYZEN_Database_SQLite_Path=bak.db

# =========================================================================
# Redis
# =========================================================================
# XYZEN_Redis_HOST=localhost
# XYZEN_Redis_PORT=6379
# XYZEN_Redis_DB=0
# XYZEN_Redis_PASSWORD=

# =========================================================================
# OSS (MinIO / S3-compatible)
# =========================================================================
# XYZEN_OSS_Endpoint=http://host.docker.internal:9000
# XYZEN_OSS_PublicEndpoint=http://localhost:9000
# XYZEN_OSS_AccessKey=minioadmin
# XYZEN_OSS_SecretKey=minioadmin
# XYZEN_OSS_BucketName=xyzen
# XYZEN_OSS_Secure=False
# XYZEN_OSS_Region=us-east-1
# XYZEN_OSS_MaxUserStorageBytes=1073741824
# XYZEN_OSS_MaxFileUploadBytes=104857600
# XYZEN_OSS_MaxUserFileCount=10000

# =========================================================================
# Admin (Redemption)
# =========================================================================
# XYZEN_Admin_secret=change-this-admin-secret-key-in-production

# =========================================================================
# Lab
# =========================================================================
# XYZEN_Lab_Api=
# XYZEN_Lab_Timeout=30

# =========================================================================
# SearXNG
# =========================================================================
# XYZEN_SearXNG_Enable=True
# XYZEN_SearXNG_BaseUrl=http://127.0.0.1:8080
# XYZEN_SearXNG_Timeout=30
# XYZEN_SearXNG_DefaultCategories=general
# XYZEN_SearXNG_DefaultEngines=
# XYZEN_SearXNG_MaxResults=10

# =========================================================================
# MCP Providers
# =========================================================================
# XYZEN_MCP_Enabled=True
# XYZEN_MCP_Smithery_Name=smithery
# XYZEN_MCP_Smithery_Key=

# =========================================================================
# Dynamic MCP Server
# =========================================================================
# NOTE: Some fields in `DynamicMCPConfig` use underscores in field names (e.g. `cpu_count`).
# With nested parsing, underscores split levels, so those fields are not reliably configurable via env without adding aliases.
# XYZEN_DynamicMCP_name=DynamicMCPServer
# XYZEN_DynamicMCP_version=1.0.0
# XYZEN_DynamicMCP_host=0.0.0.0
# XYZEN_DynamicMCP_port=3001
# XYZEN_DynamicMCP_transport=sse
# XYZEN_DynamicMCP_kubeNamespace=bohrium

# =========================================================================
# LLM (Multi-provider)
# =========================================================================
# Enabled providers (comma-separated): azure_openai,openai,google,google_vertex,gpugeek,qwen
# XYZEN_LLM_providers=

# --- Azure OpenAI ---
# XYZEN_LLM_AzureOpenAI_key=
# XYZEN_LLM_AzureOpenAI_endpoint=
# XYZEN_LLM_AzureOpenAI_version=
# XYZEN_LLM_AzureOpenAI_deployment=gpt-4o

# --- OpenAI ---
# XYZEN_LLM_OpenAI_key=
# XYZEN_LLM_OpenAI_endpoint=
# XYZEN_LLM_OpenAI_deployment=gpt-4o

# --- Google GenAI ---
# XYZEN_LLM_Google_key=
# XYZEN_LLM_Google_endpoint=https://generativelanguage.googleapis.com
# XYZEN_LLM_Google_deployment=

# --- Google Vertex ---
# `key` is base64-encoded service account JSON.
# XYZEN_LLM_GoogleVertex_key=
# XYZEN_LLM_GoogleVertex_project=
# XYZEN_LLM_GoogleVertex_deployment=

# --- GPUGeek ---
# XYZEN_LLM_GPUGeek_key=
# XYZEN_LLM_GPUGeek_endpoint=https://api.gpugeek.com/v1
# XYZEN_LLM_GPUGeek_deployment=

# --- Qwen ---
# XYZEN_LLM_Qwen_key=
# XYZEN_LLM_Qwen_endpoint=https://dashscope.aliyuncs.com/compatible-mode/v1
# XYZEN_LLM_Qwen_deployment=qwen-max

# =========================================================================
# LLM (Legacy Single-provider)
# =========================================================================
# Provider: azure_openai | openai | google | google_vertex | gpugeek | qwen
# XYZEN_LLM_provider=
# XYZEN_LLM_key=
# XYZEN_LLM_endpoint=
# XYZEN_LLM_version=
# XYZEN_LLM_deployment=
